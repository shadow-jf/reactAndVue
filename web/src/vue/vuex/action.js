import * as type from './type';

const actions = {
  showhight: (contex) => {
    contex.commit(type.showhight);
  },
  showhight2: (contex) => {
    contex.commit(type.showhight2);
  },
};

export default actions;
